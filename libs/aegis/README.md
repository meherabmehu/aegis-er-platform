# aegis-core

Pure-Python core library for AEGIS-ER: types, geospatial primitives, routing,
priority scoring, hybrid assignment solver, explainability, and an in-memory
world model that services and the simulator share.

Install (editable, dev):

```bash
pip install -e '.[dev]'
```

Run tests:

```bash
pytest -q
```
